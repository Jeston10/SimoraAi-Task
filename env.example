# ============================================
# REQUIRED - Must have for basic functionality
# ============================================

# Speech-to-Text (STT) Provider - Choose ONE:
# Option 1: Hugging Face (FREE, RECOMMENDED) - Open source Whisper model
# Option 2: OpenAI (Paid) - Fallback option

# Hugging Face API Key (FREE - RECOMMENDED)
# Get from: https://huggingface.co/settings/tokens
# Steps:
# 1. Go to https://huggingface.co/ and create account (free)
# 2. Go to https://huggingface.co/settings/tokens
# 3. Click "New token"
# 4. Select "Read" permission
# 5. Copy the token (starts with hf_)
# FREE: Unlimited requests for open-source models
HUGGINGFACE_API_KEY=hf_your-huggingface-token-here

# OpenAI API Key (Paid - Optional fallback)
# Get from: https://platform.openai.com/api-keys
# Steps:
# 1. Go to https://platform.openai.com/
# 2. Sign up or log in
# 3. Go to https://platform.openai.com/api-keys
# 4. Click "Create new secret key"
# 5. Copy the key (starts with sk-)
# 6. Add credits at https://platform.openai.com/account/billing
# PAID: ~$0.006 per minute of audio
# OPENAI_API_KEY=sk-your-openai-api-key-here

# STT Provider Selection (optional)
# Options: "huggingface", "openai", "auto"
# Default: "auto" (prefers Hugging Face if available)
STT_PROVIDER=auto

# ============================================
# Optional Configuration
# ============================================

# File Storage Provider - Choose ONE:
# Option 1: Supabase Storage (FREE, RECOMMENDED) - Open source, generous free tier
# Option 2: Vercel Blob Storage (Paid) - Fallback option

# Supabase Storage (FREE - RECOMMENDED)
# Get from: https://supabase.com/
# Steps:
# 1. Go to https://supabase.com/ and create account (free)
# 2. Create a new project
# 3. Go to Settings > API
# 4. Copy "Project URL" (SUPABASE_URL) - format: https://xxxxx.supabase.co
# 5. Copy "service_role" key (SUPABASE_SERVICE_ROLE_KEY) - Keep this secret!
#    - This is the "service_role" key, NOT the "anon" key
#    - It's found in Settings > API > Project API keys > service_role (secret)
# 6. Go to Storage > Create bucket named "videos" (or set SUPABASE_STORAGE_BUCKET)
# 7. IMPORTANT: Set bucket to "Public" for public access
#    - Go to Storage > [your bucket] > Settings
#    - Toggle "Public bucket" to ON
#    - This allows files to be accessed via public URLs
# 8. (Optional) The bucket will be auto-created if it doesn't exist, but making it public manually is recommended
# FREE: 1GB storage, 2GB bandwidth/month
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_STORAGE_BUCKET=videos

# Vercel Blob Storage Token (Paid - Optional fallback)
# Get from: Vercel Dashboard > Storage > Blob
# Steps:
# 1. Go to https://vercel.com/ and create account
# 2. Create a new project
# 3. Go to Storage tab > Create Database > Select "Blob"
# 4. Go to Storage settings > Copy the token (starts with vercel_blob_rw_)
# VERCEL_BLOB_STORAGE_TOKEN=vercel_blob_rw_your-token-here

# Storage Provider Selection (optional)
# Options: "supabase", "vercel", "auto"
# Default: "auto" (prefers Supabase if available)
STORAGE_PROVIDER=auto

# AssemblyAI API Key (Optional - fallback STT service)
# Get your API key from: https://www.assemblyai.com/
ASSEMBLYAI_API_KEY=your-assemblyai-api-key-here

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
MAX_VIDEO_SIZE_MB=100
MAX_VIDEO_DURATION_SECONDS=600

# Logging Configuration
# Options: debug, info, warn, error
# Default: debug (development), info (production)
LOG_LEVEL=info

# ============================================
# Error Monitoring (Optional)
# ============================================

# Sentry DSN for error tracking (OPTIONAL)
# Steps:
# 1. Go to https://sentry.io/ and create account
# 2. Create new project (select Next.js)
# 3. Copy the DSN (format: https://xxxxx@xxxxx.ingest.sentry.io/xxxxx)
# 4. Install: npm install @sentry/nextjs
# 5. Run: npx @sentry/wizard@latest -i nextjs
# Or manually uncomment and configure in lib/monitoring.ts
NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx

# ============================================
# Remotion Configuration (if using AWS for rendering)
# ============================================
# REMOTION_AWS_ACCESS_KEY_ID=your-aws-access-key
# REMOTION_AWS_SECRET_ACCESS_KEY=your-aws-secret-key

